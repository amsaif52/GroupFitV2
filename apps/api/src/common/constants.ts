/** API route prefixes per division. Kept in API only; web/RN use shared for their constants. */
export const API_PREFIXES = {
  CUSTOMER: '/customer',
  TRAINER: '/trainer',
  ADMIN: '/admin',
} as const;
